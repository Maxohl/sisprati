<% layout('layouts/boilerplate') %> 
    <% var i=1 %> 
    <% requiData.forEach(function(result) { %> 
 
<div class="row">
    <div class="col-6 offset-3">
        <div class="card mb-3">        
            <div class="card-body">
                <h5 class="card-title"><%= Navio %></h5>
            </div>
            <ul class="list-group list-group-flush">
                <li class="list-group-item"><b>Serviço:</b> <%= result.Servico %></li>
                <li class="list-group-item"><b>Data:</b> <%= converte(result.Data) %> </li>
                <li class="list-group-item"><b>Viagem:</b> <%= result.Viagem %></li>                
                <li class="list-group-item"><b>Berço:</b> <%= result.Berco %></li>
                <li class="list-group-item"><b>Posição:</b> <%= result.Posicao_Berco %></li>
                <li class="list-group-item"><b>Obs:</b> <%= result.OBS %></li>
                <li class="list-group-item"><b>Faturamento:</b> <%= result.Fatu %></li>
            </ul>
            <div class="card-body">
                <a href="/condicionada/edit/<%= result.ID %>" class="card-link btn btn-info text-white">Editar Requisição Condicionada</a>
                    <form class="d-inline" action="/condicionada/<%= result.ID %>?_method=DELETE" method="POST">
                        <button class="btn btn-danger">Cancelar Requisição Condicionada</button>
                    </form>
            </div>
            <div class="card-footer text-muted">
                <a href="/condicionada">Voltar</a>
            </div>
        </div>
    </div>
</div>
<% function converte(date){ %> 
    <% date = new Date(date) %>
    <% return date.getUTCDate() + '/' + ('00' + (date.getUTCMonth()+1)).slice(-2) + '/' + ('0000' + date.getUTCFullYear()).slice(-4) %>
    <% } %>   

      <% i++ })  %> 